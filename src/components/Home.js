import React from "react";

const Home = () => (
  <section>
    <h1 className="section-title section-title-light">Home</h1>
    <div>Welcome to Bss. This is a not BBS.</div>
  </section>
);

export default Home;
